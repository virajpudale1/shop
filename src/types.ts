export interface CartProps {
  cart: any; // Replace 'any' with the actual type of your cart
  setCart: React.Dispatch<React.SetStateAction<any>>; // Replace 'any' with the actual type of your cart
}
